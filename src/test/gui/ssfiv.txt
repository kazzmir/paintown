(select-screen
  (name "ssfiv-select")
  (animation
    (location 0)
    (image 0 "menu/paintown.png")
    (velocity -.001 0)
    (frame
      (image 0)
      (time -1)))
  
  ; Grid type
  ; - simple-list
  ; - grid-list
  (grid-list 
    (grid-size 12 3)
    (layout "static")
    (cell-dimensions 35 35)
    (cell-spacing -3 -1)
    (cell-margins 10 10)
    (cursors 1)
    (wrap true))
  ; Auto populate the grid ?
  (auto-populate true)
  ; Directory in which to populate from
  (auto-populate-directory "players")
  ; Window coordinates where list will be
  ; x y width height
  (list-window 50 240 550 200)
  ; List depth
  (list-depth foreground middle)
  ; Profile window is where the character avatar or animation can be drawn make one per cursor
  (profile-window 0 -10 320 480)
  ; Depth in which to display the profiles
  (profile-depth foreground bottom)
  ; text Message
  (text (message "Character Select") (location 160 45) (color 0 255 255) (depth background top) (font-dimensions 40 40) (justification center))
  (text (message "Character%s") (location 5 215) (color 0 0 0) (depth background top) (font-dimensions 35 35) (justification right) (profile-association 0))
  (text (message "Character%s") (location 0 210) (color 255 255 255) (depth foreground bottom) (font-dimensions 35 35) (justification right) (profile-association 0))
  
  ; FIXME Remove or replace these later (only for testing)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  (add)
  
  ; Font
  ; (font somefont.ttf 32 32)
  ; Font dimensions
  (font-dimensions 35 35)
)
