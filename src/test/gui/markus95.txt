(select-screen
  (name "markus95-select")
  (animation
    (location 0)
    (image 0 "menu/paintown.png")
    (velocity -.001 0)
    (frame
      (image 0)
      (time -1)))
  
  ; Grid type
  ; - simple-list
  ; - grid-list
  (grid-list 
    (grid-size 8 2)
    (layout "infinite-horizontal")
    (cell-dimensions 55 55)
    (cell-spacing 0 0)
    (cell-margins 20 325)
    (cursors 1)
    (wrap true))
  ; Arrows 
  ; LEFT
  (more "low" (location 40 220) (dimensions 40 40) (direction left) (depth foreground bottom) (images (image (file "android/arrowleft.png") (time -1))))
  ; RIGHT
  (more "high" (location 600 220) (dimensions 40 40) (direction right) (depth foreground bottom) (images (image (file "android/arrowright.png") (time -1))))
  
  ; Cells
  (cell (name back) 
    (images 
        (image (file "select/back.png") (time -1))))
  (cell (name top) 
    (images 
        (image (file "select/front.png") (time -1))))
  (cell (name select0) 
    (images 
        (image (file "select/select0.png") (time -1))))
  (cell (name select1) 
    (images 
        (image (file "select/select1.png") (time -1))))
  (cell (name select-alternative) 
    (images 
        (image (file "select/selectalt.png") (time -1))))
  ; Auto populate the grid ?
  (auto-populate true)
  ; Directory in which to populate from
  (auto-populate-directory "players")
  ; Window coordinates where list will be
  ; x y width height
  (list-window 20 20 620 460)
  ; Profile window is where the character avatar or animation can be drawn make one per cursor
  (profile-window 60 140 180 340)
  ; text Message
  (messages
    ;(text (message "Pick a Player") (location 10 25) (color 255 255 255) (depth background top) (font-dimensions 35 35) (justification right))
    (text (message "%s") (location 205 165) (color 0 0 0) (depth background top) (font-dimensions 45 45) (justification right) (profile-association 0))
    (text (message "%s") (location 200 160) (color 255 255 255) (depth foreground bottom) (font-dimensions 45 45) (justification right) (profile-association 0))
    )
  
  ; Add player by relative path
  (add "players/akuma/akuma.txt")
  (add "players/akuma/akuma.txt")
  (add "players/akuma/akuma.txt")
  (add "players/akuma/akuma.txt")
  (add "players/akuma/akuma.txt")
  
  ; Font
  ; (font somefont.ttf 32 32)
  ; Font dimensions
  (font-dimensions 35 35)
)
