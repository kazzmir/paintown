#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1
export SDL=1

%:
		dh $@

override_dh_auto_test:
		echo "Skipping tests"

override_dh_auto_install:
		@-mkdir -p /usr/bin/games/
		@cp paintown /usr/bin/games/
