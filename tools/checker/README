Get gccxml

 $ cvs -d :pserver:anoncvs@www.gccxml.org:/cvsroot/GCC_XML login
 $ cvs -d :pserver:anoncvs@www.gccxml.org:/cvsroot/GCC_XML co gccxml

Build it with cmake

 $ cd gccxml
 $ mkdir b
 $ cd b
 $ cmake .. -DCMAKE_INSTALL_PREFIX:PATH=`pwd`/../install
 $ make
 $ make install
 $ ls ../install/bin/gccxml
