#!/bin/sh

for directory in $(find . -type d  ! -regex '.*.svn.*')
do
        # \ls "$directory" > "$directory/directory.txt"
        \ls -l "$directory" | awk -F" " '{printf "%s %s\n", $8, $5}' > "$directory/directory.txt"
done
