name: Compile Paintown

on:
  push:
    branches: [ sdl2 ]

jobs:
  build:
    name: Compile
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Run build
        run: |
          ./easy-compile-ubuntu
